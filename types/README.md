# types/

`database.types.ts` is generated from the Supabase schema — never hand-edit it.
Regenerate after every migration:  npm run gen:types
One-time setup:  npx supabase login  then  npx supabase link --project-ref <your-project-ref>
